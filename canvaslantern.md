# TechResourceHub

TechResourceHub 是一个面向技术开发者、架构师与 IT 决策者的外链资源导航与聚合系统。该项目不存储任何实体内容，而是通过结构化分类、状态监控与访问加速机制，为用户提供高质量、高可用性的外部技术资源快速访问入口。项目定位为“技术资源的第一跳板”，尤其适用于需要频繁查阅多源文档、工具站、行业资讯与社区讨论的技术团队。

目标用户包括：日常维护大量书签的研发人员、需要统一团队知识入口的技术负责人、以及希望降低外部资源查找成本的企业内部开发者门户。通过本项目的统一入口与健康检查机制，用户可避免死链、过期资源或不可靠来源的干扰，提升信息获取效率与安全性。

## 功能概览

- **资源分类导航**：按技术领域、资源类型、使用频率等多维度对链接进行分组，提供清晰的浏览路径。

- **可用性主动探测**：定时对收录资源发起 HTTP/HTTPS 请求，检测状态码与响应时间，自动标记异常链接。

- **访问加速中转**：针对部分地区访问缓慢的资源，提供可选的反向代理或预取缓存策略，改善访问体验。

- **标签与全文检索**：为每个资源赋予技术标签（如 Kubernetes、Python、DevOps），支持关键词快速定位。

- **访问统计与热度排序**：记录资源被点击次数与最近访问时间，按热度动态调整展示顺序。

- **RSS 订阅聚合**：对于支持 RSS 的技术博客与更新日志，自动抓取并展示最新条目摘要。

- **私有化部署支持**：提供 Docker 镜像与一键部署脚本，允许用户在内网环境独立运行整套资源导航系统。

## 应用场景

- **技术团队内部知识库门户**：团队 Leader 可将常用 CI/CD 工具、监控面板、日志平台、代码仓库等内部与外部资源统一收录，新成员入职时仅需访问本系统即可获得所有必要入口。

- **个人开发环境起始页**：开发者可将本系统设置为浏览器新标签页或默认首页，每天开始工作前快速查看关注的资源更新状态，直接跳转至需要的文档或工具。

- **技术社区资源分享站**：开源社区或线下 meetup 组织者可使用本系统集中整理演讲材料、视频回放、相关项目仓库与延伸阅读链接，便于活动参与者后续查阅。

- **多机房/多区域资源镜像选择**：运维团队可为同一资源配置多个备用域名或 CDN 地址，当主地址不可达时自动引导用户切换，保障业务连续性。

## 快速开始

以下命令演示了如何在 Linux 或 macOS 环境中从源码启动 TechResourceHub。

```bash
# 克隆项目仓库
git clone https://github.com/tech-resource-hub/techresourcehub.git

# 进入项目根目录
cd techresourcehub

# 安装项目依赖（使用 Python 3.10+ 与 pip）
pip install -r requirements.txt

# 初始化本地配置文件（复制示例配置）
cp config.example.yaml config.yaml

# 运行开发服务器（默认监听 8080 端口）
python app.py --port 8080
```

启动成功后，访问 `http://localhost:8080` 即可看到资源导航主界面。首次启动时系统会自动导入预置的默认资源列表，并开始后台可用性探测。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10 及以上 | 核心运行环境，用于处理 HTTP 请求、模板渲染与后台任务 |
| pip | 22.0 及以上 | Python 包管理器，用于安装 requirements.txt 中声明的依赖 |
| SQLite | 3.35 及以上 | 默认内置数据库，存储资源条目、访问日志与探测结果 |
| Redis | 6.0 及以上 | 可选依赖，启用缓存与分布式任务队列时需要 |
| Docker | 20.10 及以上 | 可选依赖，用于容器化部署与编排 |
| Git | 2.25 及以上 | 用于克隆代码仓库及版本管理 |
| 操作系统 | Linux / macOS / Windows WSL2 | 推荐 Linux 生产环境，开发环境支持主流系统 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | `docs/user/` | 如何添加个人书签、如何自定义分类视图、如何使用检索与过滤功能 |
| 管理员手册 | `docs/admin/` | 如何配置探测频率、如何添加/批量导入资源、如何查看健康状态面板 |
| 部署运维 | `docs/ops/` | 如何通过 Docker 或 Kubernetes 部署生产实例、如何配置外部数据库与缓存 |
| 开发贡献 | `docs/contrib/` | 如何扩展新的资源解析器、如何修改前端主题、如何提交 Pull Request |

## 资源列表

### 综合技术门户

<code>moliaoyue.com.cn</code>

<code>tongchengyue.com.cn</code>

### 垂直领域资源

<code>yueaiwang.com.cn</code>

<code>jimonvren.net.cn</code>

### 行业工具与资讯

<code>chuguiriji.com.cn</code>

<code>gaoqingwumaziyuan.com.cn</code>

<code>ribennvyoutuijian.com.cn</code>

## 项目结构

```
techresourcehub/
├── app.py                   # 应用入口，初始化 Flask 与后台调度器
├── config.yaml              # 用户可编辑的配置文件（含数据库路径、探测间隔）
├── requirements.txt         # Python 依赖列表
├── docker-compose.yml       # 容器编排文件（含 app + redis + 可选 proxy）
├── Dockerfile               # 生产环境镜像构建脚本
├── core/                    # 核心业务逻辑
│   ├── resource_manager.py  # 资源增删改查、分类树维护
│   ├── probe_engine.py      # 异步可用性探测引擎（基于 aiohttp）
│   └── cache_client.py      # Redis 缓存操作封装
├── web/                     # Web 层视图与静态资源
│   ├── routes/              # 路由蓝图（首页、分类、详情、管理后台）
│   ├── templates/           # Jinja2 模板文件
│   └── static/              # CSS, JS, 图标文件
├── scheduler/               # 定时任务模块
│   ├── jobs.py              # 定义探测、统计清理等周期任务
│   └── worker.py            # 独立任务执行进程
├── tests/                   # 单元测试与集成测试用例
│   ├── test_probe.py
│   └── test_resource.py
└── docs/                    # 完整文档（用户、管理、运维、贡献）
    ├── user/
    ├── admin/
    ├── ops/
    └── contrib/
```

## 贡献指南

1. 查阅 `docs/contrib/` 目录下的开发环境搭建文档，确保本地 Python 与 Redis 环境满足要求，并运行 `./scripts/setup_dev.sh` 完成预提交钩子安装。

2. 从 `main` 分支创建新的特性分支，分支命名建议采用 `feature/功能简述` 或 `fix/问题简述` 格式，确保分支与上游保持同步。

3. 编写或修改代码时，请遵循 PEP 8 编码规范，并为新增函数或类添加 docstring。若涉及资源解析逻辑变化，需同步补充 `tests/` 下的对应测试用例，保证测试覆盖率不低于 80%。

4. 提交前执行 `make lint` 与 `make test` 进行静态检查与单元测试，确保无错误与警告。提交信息请使用清晰的语言描述变更内容与动机。

5. 发起 Pull Request 至 `main` 分支，并在 PR 描述中关联相关 Issue（若有）。PR 需要至少一位维护者审阅通过后方可合并。

## 常见问题

**问：系统检测到某个资源不可用时，会自动重试吗？**

答：是的。探测引擎对每个资源默认执行 3 次重试（间隔 5 秒），仅当所有重试均失败后才标记为“异常”。此外，用户可手动点击管理后台的“立即探测”按钮进行即时检查。探测结果会记录历史趋势，便于区分临时故障与持续性宕机。

**问：能否导入我已有的浏览器书签或其它导航站的数据？**

答：支持。管理后台提供了 CSV 与 JSON 格式的批量导入功能，您只需按照模板文件整理标题、URL、分类、标签等字段即可完成迁移。对于 Firefox / Chrome 导出的 HTML 书签文件，项目提供了独立的转换脚本 `tools/convert_bookmark.py` 进行自动解析。

**问：本项目是否适合在内网环境部署，完全离线运行？**

答：完全可以。除首次启动时需要联网下载依赖包外，本系统不强制依赖任何外部在线服务。您可以将所有静态资源与图标文件本地化，并将探测功能调整为仅检测内网地址。Docker 镜像也支持通过 `--offline` 启动参数禁用所有外部 CDN 请求。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:12
