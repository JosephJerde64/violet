# Hanyu Link Navigator

Hanyu Link Navigator 是一个面向中文视听资源与技术爱好者的轻量级导航与索引系统。该项目并非内容托管平台，亦不存储或分发任何受版权保护的媒体文件，而是专注于对互联网上公开可访问的中文字幕、在线播放、高清影视等技术演示与学习资源进行结构化整理与分类索引，帮助开发者、内容研究者以及语言学习群体快速定位其所需的外部参考链接。

项目的主要目标用户包括：从事流媒体技术研究的工程师、需要大量中文视听素材进行自然语言处理模型训练的数据科学家、以及希望通过真实影视语料提升中文听力水平的语言学习者。通过提供清晰、可维护的链接分类体系与状态监测机制，本项目有效降低了用户在碎片化网络环境中筛选可用资源的时间成本，提升了信息检索效率。

## 功能概览

- **多维度资源分类索引**：按照资源类型、内容主题、访问协议等多个维度对收录链接进行标签化分类，支持用户按需筛选与快速跳转。

- **链接可用性健康检查**：内置简单的 HTTP 状态码检测模块，定期对收录的 URL 进行连通性测试，并在界面中标明异常链接，避免用户访问失效地址。

- **自定义分类标签系统**：允许用户为每条资源添加自定义标签，便于构建个性化的资源分组方案，满足不同研究方向或学习阶段的特定需求。

- **全文搜索与模糊匹配**：基于简单的倒排索引机制，支持对资源标题、描述、标签及域名进行关键词检索，并容忍拼写错误或近似匹配。

- **访问统计与热度排序**：记录每条链接的被点击次数，支持按访问频率或最近添加时间对资源列表进行动态排序，帮助用户发现热门或最新资源。

- **RSS 订阅源自动生成**：为资源分类和标签自动生成 RSS 2.0 格式的订阅链接，方便用户通过阅读器及时追踪特定类别下的新增资源。

- **响应式移动端适配界面**：前端页面基于 CSS Flexbox 与 Grid 布局构建，在桌面、平板与手机设备上均能提供流畅的浏览与操作体验。

## 应用场景

- **流媒体技术研究人员的参考素材库**：研究人员在开发或测试新的视频播放器、字幕渲染引擎或流媒体传输协议时，可使用本导航项目中收录的公开在线播放链接作为测试样本，验证软件在不同码率、格式和网络条件下的兼容性。

- **语言学习者的听力训练辅助工具**：中文学习者可以通过本导航快速找到带有中文字幕的影视内容在线播放地址，结合外部播放器的变速与复读功能进行精听与跟读练习，从而在真实语境中提升听力理解水平。

- **数据科学家构建多模态语料库的入口**：NLP 或计算机视觉研究团队可将本导航中的资源链接作为数据采集管道的数据源入口，通过自动化脚本批量获取字幕文本或视频元数据，用于训练语音识别模型或字幕翻译系统。

- **内容策展人整理主题资源合集**：影视博主或教育内容策展人可利用本导航的分类与标签功能，围绕特定主题（如中国纪录片、经典老电影、少儿动画等）创建公开的资源合集页面，并分享给其受众群体。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境。请确保系统已安装 Git 与 Node.js（版本 16.x 或以上）。

```bash
# 克隆项目仓库至本地
git clone https://github.com/hanyu-link-navigator/hln-core.git

# 进入项目工作目录
cd hln-core

# 安装项目依赖（使用 npm）
npm install

# 以开发模式启动本地服务，默认监听端口 3000
npm run dev
```

执行完毕后，在浏览器中访问 `http://localhost:3000` 即可查看导航站点首页。若需构建生产版本，请执行 `npm run build`，产物将输出至 `dist` 目录。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 16.x 或更高 | JavaScript 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 8.x 或更高 | Node.js 包管理器，用于安装项目依赖项 |
| Git | 2.30 或更高 | 分布式版本控制系统，用于克隆仓库与提交变更 |
| 现代网页浏览器 | 最新两个主要版本 | 用于访问前端界面，推荐 Chrome / Firefox / Edge |
| 网络连接 | 稳定宽带 | 用于首次安装依赖包以及访问外部资源链接 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 开发与运行环境，Windows 原生支持可能受限 |
| SQLite3 | 内置于依赖中 | 轻量级嵌入式数据库，用于存储资源索引与统计数据 |
| ESLint | 开发依赖 | 代码质量检查工具，推荐在提交前运行 lint 脚本 |

## 文档导航

| 层面 | 目录 / 文件 | 回答的问题 |
| :--- | :--- | :--- |
| 用户手册 | `docs/user-guide/` | 如何添加新资源链接？如何创建自定义分类？如何导入/导出我的资源列表？ |
| 开发者指南 | `docs/developer-guide/` | 项目的架构设计是怎样的？如何扩展健康检查模块？如何贡献代码？ |
| API 参考 | `docs/api-reference/` | 后端提供了哪些 RESTful 接口？请求参数与响应格式如何？如何通过 API 批量更新链接状态？ |
| 部署运维 | `docs/deployment/` | 如何将项目部署到生产服务器？支持哪些托管平台？如何配置环境变量？ |
| 设计文档 | `docs/design/` | 界面设计遵循什么规范？色彩与排版体系如何？可访问性支持达到何种级别？ |
| 测试指南 | `docs/testing/` | 单元测试如何编写与运行？如何对链接检测模块进行模拟测试？覆盖率要求是多少？ |

## 资源列表

本部分列出项目当前收录的全部外部导航链接。所有 URL 均按原始输入形式原样呈现，未做任何协议补全、域名规范化或路径修正。用户在使用时应自行确认链接的可访问性与安全性。

### 中文字幕在线播放类

- <code>zuixinzhongwenzimuzaixian.com.cn</code>

- <code>zhongwenzaixianguankanshipin.com.cn</code>

- <code>zhongwenzimuzhuanqu.com.cn</code>

### 中文字幕高清观看类

- <code>zhongwenzimuzaixianshipinguankan.com.cn</code>

- <code>zhongwenzimugaoqingzaixianguankan.com.cn</code>

### 中文字幕在线播放与视频类

- <code>zhongwenzimuzaixianbofangshipin.com.cn</code>

- <code>zaixianzhongwenzimushipin.com.cn</code>

以上链接按照域名语义进行了粗略分组，实际使用中用户可依据本导航系统的标签功能对其添加更细致的分类标记。

## 项目结构

```
hln-core/
├── src/                           # 源代码主目录
│   ├── client/                    # 前端界面源码
│   │   ├── components/            # 可复用 UI 组件 (导航卡片、搜索栏、标签过滤器)
│   │   ├── pages/                 # 页面级组件 (首页、分类页、详情页、统计页)
│   │   ├── styles/                # 全局样式表与主题变量 (CSS 与 PostCSS)
│   │   └── utils/                 # 前端工具函数 (日期格式化、URL 解析、防抖)
│   ├── server/                    # 后端服务源码
│   │   ├── api/                   # RESTful 路由与控制器 (资源 CRUD、标签管理、统计上报)
│   │   ├── models/                # 数据模型定义 (资源、标签、点击记录、健康检查结果)
│   │   ├── services/              # 业务逻辑层 (链接检测服务、RSS 生成器、搜索索引器)
│   │   ├── middleware/            # 中间件 (请求日志、跨域处理、错误捕获、速率限制)
│   │   └── workers/               # 后台工作进程 (定时健康检查队列、统计聚合任务)
│   ├── shared/                    # 前后端共享代码
│   │   ├── constants/             # 常量定义 (资源类型枚举、默认标签、状态码映射)
│   │   ├── types/                 # TypeScript 类型声明与接口定义
│   │   └── validators/            # 输入校验规则 (Joi / Zod 模式)
│   └── config/                    # 配置文件目录
│       ├── default.json           # 默认配置项 (端口、数据库路径、检测间隔)
│       ├── production.json        # 生产环境覆盖配置
│       └── development.json       # 开发环境覆盖配置
├── tests/                         # 测试文件目录
│   ├── unit/                      # 单元测试 (服务层与工具函数)
│   ├── integration/               # 集成测试 (API 端点与数据库交互)
│   └── fixtures/                  # 测试固定数据 (模拟资源列表与标签)
├── docs/                          # 项目文档 (详见上方文档导航)
├── scripts/                       # 辅助脚本 (数据库迁移、种子数据填充、部署工具)
├── public/                        # 静态资源 (favicon、robots.txt、sitemap.xml)
├── package.json                   # npm 依赖清单与脚本定义
├── tsconfig.json                  # TypeScript 编译配置
├── .eslintrc.js                   # ESLint 代码规范配置
├── .prettierrc                    # Prettier 代码格式化配置
├── Dockerfile                     # 容器化构建文件
├── docker-compose.yml             # 本地开发环境容器编排
└── README.md                      # 项目入口文档 (即本文档)
```

## 贡献指南

我们欢迎并感谢所有形式的贡献，包括但不限于提交问题报告、改进文档、增加新功能以及优化现有代码。请遵循以下步骤参与项目开发：

1.  **查阅议题与公告**：在提交任何代码或文档变更之前，请先浏览项目的 GitHub Issues 页面，查看是否存在相关的待办事项或已知问题。如计划开展较大规模的重构或新增功能，建议先创建一个讨论议题，与维护者和其他贡献者沟通设计思路，避免无效工作。

2.  **派生仓库并创建特性分支**：将本项目派生至您个人的 GitHub 账户，然后在本地克隆派生后的仓库。请基于最新的 `main` 分支创建一个新的分支，分支命名建议遵循 `feature/` 或 `fix/` 前缀加简短描述的形式，例如 `feature/add-rtsp-support` 或 `fix/health-check-timeout`。

3.  **编写代码与测试**：在您的分支上进行开发时，请确保代码风格与项目现有规范保持一致（ESLint 与 Prettier 配置已集成）。对于新增的功能模块或修复的关键缺陷，请编写相应的单元测试或集成测试用例，确保测试覆盖率达到预期阈值。提交前请运行 `npm run lint` 与 `npm test` 验证无误。

4.  **更新文档与变更日志**：若您的变更涉及到用户可见的功能调整或配置项变化，请同步更新 `docs/` 目录下的相关文档，并在 `CHANGELOG.md` 文件中记录变更摘要，注明变更类型（新增、修复、废弃或破坏性变更）。

5.  **发起合并请求**：将您的分支推送至派生仓库后，通过 GitHub 界面发起合并请求至本项目的 `main` 分支。请在合并请求描述中清晰说明变更目的、实现方案以及测试情况，并关联相关的议题编号（如有）。维护者会在收到请求后进行代码审查，并在一至两个工作日内给予反馈。

## 常见问题

**问：本项目是否提供实际的视频文件或字幕文件下载？**

答：不提供。本项目仅为导航索引系统，所有收录的 URL 均指向第三方网站。项目本身不存储、缓存、代理或传输任何视频、音频或字幕文件内容。用户通过本导航访问的任何外部资源，其版权与内容责任均属于原始提供方。

**问：部分收录的链接无法访问或响应缓慢，如何处理？**

答：本导航内置了定期健康检查机制，会自动标记检测失败的链接。但受限于网络环境与外部站点的反爬策略，检测结果可能存在一定的误报或延迟。如果您发现某个链接长期不可用，可在前端界面点击“报告失效”按钮提交反馈，维护人员将在核实后从索引中移除或更新该链接。

**问：如何向本导航项目中添加新的资源链接？**

答：您可以通过两种方式提交新链接。一是直接在前端界面使用“提交资源”表单，填写标题、URL、分类和标签信息，经后台自动审核格式后即可入库。二是通过 GitHub 发起合并请求，在 `scripts/seed-data.json` 文件中按格式添加新条目并附上来源说明。对于批量提交或包含复杂元数据的资源，建议使用后一种方式。

**问：部署本项目需要配置外部数据库吗？**

答：默认情况下，项目使用 SQLite3 嵌入式数据库，无需额外安装或配置外部数据库服务，适合中小规模使用场景。如果您预计收录的资源数量超过一万条，或需要多节点读写高可用架构，则建议在配置文件中切换至 PostgreSQL，并按照 `docs/deployment/database.md` 中的指南进行迁移与调优。

## 许可证

本项目采用 MIT 许可证进行开源授权。您被允许自由使用、复制、修改、合并、发布、分发、再授权及销售本软件的副本，但在所有副本中必须保留原始版权声明及许可声明。本软件按“现状”提供，不附带任何明示或暗示的担保，包括但不限于适销性、特定用途适用性和非侵权性的担保。有关完整条款，请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:12
