# ResourceHub

ResourceHub 是一个面向技术开发者与内容研究者的外链资源聚合与导航系统。本项目定位于将分散在网络各处的优质工具、文档、媒体资源进行结构化整理与分类展示，帮助用户快速定位所需外部资源，避免在海量信息中重复检索。项目本身不存储任何资源文件，仅提供索引与跳转服务，适用于搭建个人或团队内部的技术资源收藏夹、项目依赖外链管理面板、或轻量级知识库入口。

目标用户包括全栈工程师、技术文档撰写者、运维人员以及需要频繁引用外部多媒体素材的内容策划团队。通过统一的入口，用户可减少重复搜索成本，提升资源复用效率，并借助清晰的分类与注释，实现外链的持久化维护与团队共享。

## 功能概览

**外链分类导航** 支持按资源类型、使用频率、来源域名等维度对收录链接进行分组展示，便于用户按场景快速过滤。

**链接状态检测** 自动对已收录的外链进行可访问性检查，并在管理后台标注异常状态，降低维护成本。

**搜索与过滤** 提供基于关键词、标签、域名后缀的实时搜索功能，支持模糊匹配，提升定位效率。

**访问统计看板** 记录每个外链的点击次数、最近访问时间及来源 IP 区域分布，辅助用户判断资源热度。

**标签体系管理** 允许用户为每个链接自定义多个标签（如 "video"、"documentation"、"tool"），形成多维分类网络。

**数据导入导出** 支持 CSV 与 JSON 格式的资源列表批量导入导出，方便迁移或备份。

**用户权限分级** 提供管理员、编辑者、只读访客三级权限，适用于团队协作场景。

## 应用场景

技术团队内部文档库的资源引用管理：开发团队可将项目中依赖的第三方库主页、API 文档、设计稿在线地址等集中收录于 ResourceHub，并在技术文档中直接引用项目内生成的短链，避免因成员各自收藏导致版本不一致。

多媒体素材采集与整理：内容运营人员可将分散在不同视频平台的公开素材页面（如示例影片、教学片段）统一添加到 ResourceHub，配合标签区分用途（如 "预览"、"剪辑素材"、"参考案例"），提升素材检索效率。

开源项目 README 外链维护：开源维护者可使用 ResourceHub 管理项目 README 中引用的所有外部链接（如构建工具官网、依赖仓库、教程地址），当外部链接变更时，仅需在 ResourceHub 中更新，无需修改多个文档版本。

个人知识库的快捷入口：研究员或学生可将常用在线词典、学术搜索引擎、公开数据集地址集中存储，并通过搜索功能快速调用，减少浏览器书签栏的杂乱堆积。

## 快速开始

以下步骤适用于 Linux/macOS 及 Windows WSL 环境，确保系统已安装 Git 与 Node.js（版本 >= 18.0）。

```bash
# 克隆项目仓库
git clone https://github.com/resourcehub/resourcehub.git

# 进入项目目录
cd resourcehub

# 安装依赖
npm install

# 使用示例数据初始化本地数据库
npm run init-db

# 启动开发服务器（默认端口 3000）
npm run dev
```

启动成功后，访问 `http://localhost:3000` 即可进入导航面板。首次启动会自动生成管理员账号（提示见控制台输出），请及时修改默认密码。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 18.0.0 | 运行时环境，用于执行后端服务与构建脚本 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖 |
| SQLite | 3.35+ (内置) | 轻量级嵌入式数据库，用于存储链接与标签数据 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及后续更新 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 推荐使用 Unix-like 环境以获得最佳兼容性 |
| 浏览器 | Chrome / Firefox / Edge 最新版 | 用于访问管理界面，需支持 ES2020 特性 |
| 网络 | 稳定公网连接 | 用于外链状态检测及首次安装时下载依赖包 |

## 文档导航

| 层面 | 目录位置 | 回答的问题 |
|------|---------|-----------|
| 用户手册 | `docs/user-guide/` | 如何添加链接、创建标签、配置访问权限？ |
| 管理指南 | `docs/admin-guide/` | 如何执行批量导入、查看统计、设置检测策略？ |
| 开发者文档 | `docs/developer/` | 如何扩展数据模型、自定义前端主题、编写插件？ |
| API 参考 | `docs/api/` | 后端提供了哪些 RESTful 端点，请求与响应格式如何？ |
| 部署运维 | `docs/deployment/` | 如何使用 Docker 或 PM2 在生产环境部署？ |
| 常见问题 | `docs/faq/` | 收录链接失效如何处理、搜索索引如何重建？ |

## 资源列表

本项目的核心导航数据包含以下外链资源，按业务主题分组展示。所有链接均保留用户原始输入形式，未做任何协议或域名改写。

媒体资源类

<code>zhongwenzimuzaixianbofangshipin.org.cn</code>

<code>zaixianzhongwenzimushipin.org.cn</code>

<code>renqizaixianmianfeishipin.org.cn</code>

<code>zhongwenzaixianmianfeishipin.org.cn</code>

<code>mianfeishipinzhongwenzimu.org.cn</code>

<code>zaixianmianfeiguankannidongde.org.cn</code>

<code>zaixianzhongwenzimuwangzhan.org.cn</code>

## 项目结构

```
resourcehub/
├── backend/                         # 后端服务代码
│   ├── src/
│   │   ├── controllers/             # 路由控制器，处理请求与响应
│   │   ├── models/                  # 数据模型层（Link, Tag, User, Record）
│   │   ├── services/                # 业务逻辑层（检测、统计、导入导出）
│   │   ├── middlewares/             # 身份验证、日志、错误处理中间件
│   │   └── utils/                   # 工具函数（URL 规范化、日期格式化）
│   ├── routes/                      # API 路由定义（v1 版本）
│   └── app.js                       # 服务入口文件，初始化 Express 应用
├── frontend/                        # 前端单页应用
│   ├── src/
│   │   ├── pages/                   # 页面级组件（导航、详情、后台管理）
│   │   ├── components/              # 可复用 UI 组件（搜索条、标签云、统计卡片）
│   │   ├── stores/                  # Pinia 状态管理（链接列表、筛选条件、用户会话）
│   │   ├── assets/                  # 静态资源（CSS 变量、字体、图标）
│   │   └── utils/                   # 前端工具（请求封装、本地缓存）
│   └── index.html                   # 主页面模板
├── docs/                            # 项目文档（见文档导航章节）
│   ├── user-guide/
│   ├── admin-guide/
│   ├── developer/
│   ├── api/
│   └── deployment/
├── scripts/                         # 运维与工具脚本
│   ├── init-db.js                   # 初始化 SQLite 数据库表结构
│   ├── seed-data.js                 # 填充示例链接与标签数据
│   └── health-check.js              # 批量检测所有外链可用性
├── config/                          # 环境配置文件
│   ├── default.json                 # 默认配置（端口、检测间隔）
│   └── production.json              # 生产环境覆盖配置
├── tests/                           # 单元测试与集成测试
│   ├── unit/
│   └── integration/
├── .env.example                     # 环境变量模板（JWT 密钥、管理员邮箱）
├── package.json                     # npm 依赖清单与脚本定义
└── README.md                        # 项目说明文档（当前文件）
```

## 贡献指南

我们欢迎社区贡献者参与 ResourceHub 的改进。请遵循以下步骤提交变更：

1. 在 GitHub 上 Fork 本仓库至个人账号，并克隆到本地开发环境。建议在 dev 分支上进行所有开发工作，保持主分支的稳定性。

2. 选择待解决的问题或新增功能：请先查阅 `docs/developer/` 下的设计文档，确认拟修改的模块与现有架构兼容。对于重大变更（如数据模型改动），建议先提交 Issue 进行讨论。

3. 编写代码并确保通过所有测试：在提交前运行 `npm run test` 确保单元测试覆盖率不低于 80%，并执行 `npm run lint` 检查代码风格（遵循 ESLint 配置）。

4. 更新相关文档：如果变更涉及 API 端点、配置项或用户界面交互，请同步修改 `docs/` 下对应的文档文件，并补充示例。

5. 提交 Pull Request：从 dev 分支向主仓库的 dev 分支发起 PR，附上变更说明、测试结果截图及影响范围评估。待至少一位维护者审核通过后，将会合并并归入后续版本发布计划。

## 常见问题

Q: 添加外链后，前台导航面板未立即显示新链接，需要等待多久？

A: 默认情况下，系统会缓存导航数据 5 分钟以提升加载速度。若需立即生效，可在后台管理界面点击 "刷新缓存" 按钮，或调用 API 端点 `POST /api/v1/cache/refresh`。另外，请确认新链接的 "公开可见" 属性已开启。

Q: 外链状态检测显示 "不可达"，但实际在浏览器中可正常打开，是什么原因？

A: 检测服务默认使用 HEAD 请求且超时时间为 3 秒，部分服务器可能屏蔽 HEAD 方法或响应较慢。您可以在配置文件中调整检测超时时间（`config/default.json` 中的 `checker.timeout` 字段），或切换检测模式为 GET 请求。此外，若外链需要特定的 User-Agent 或 Cookie，检测服务暂不支持，此类链接建议手动标记为 "忽略检测"。

Q: 如何将现有浏览器书签批量导入 ResourceHub？

A: 目前支持导入 Netscape 格式（HTML）的书签导出文件。在后台 "导入" 页面中选择文件上传，系统会自动解析书签标题、URL 及文件夹层级，并映射为标签。若您的书签数量超过 500 条，建议分批上传以避免服务器超时。具体操作步骤请参阅 `docs/user-guide/import-export.md`。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:12
