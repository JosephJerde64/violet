# Terminus Navigator

Terminus Navigator 是一个面向技术研究人员与基础设施工程师的轻量级外部资源导航与元数据聚合系统。该项目不存储任何实体内容，仅提供结构化索引与可验证的访问指引，帮助用户在复杂网络环境中快速定位特定类型的技术文档、实验环境与分布式数据源。项目定位为“只读态资源标尺”，适用于自动化运维链、学术爬虫策略池以及网络可观测性实验床的辅助建设。

目标用户包括：网络测量研究人员、开源情报分析工程师、DevOps 流水线维护者，以及需要定期验证境外技术资源可访问性的合规团队。项目本身不依赖数据库，纯静态 Markdown 生成，支持容器化部署与 CI/CD 自动化更新。

## 功能概览

- **多源资源索引**：提供超过 200 条外部技术链接的分级分类索引，支持按域名、机构、内容类型快速筛选。
- **可验证性标记**：每条资源附带响应码预期、SSL 证书指纹预期及内容哈希校验示例，便于自动化监控。
- **轻量级代理检测**：内置简易 TCP 握手超时与 TLS 版本探测脚本，辅助判断目标域名的基础可用性。
- **定时快照对比**：支持通过 cron 触发每日资源状态快照，生成差异报告，用于变化追踪。
- **只读镜像生成**：可输出纯 HTML 静态镜像，供内网发布或离线阅读，无需动态后端。
- **标签化分类体系**：采用三级标签（地理区域/内容语种/文件类型），支持多维度交叉检索。
- **原始数据导出**：支持将索引表导出为 JSON 或 CSV 格式，便于接入外部监控系统如 Prometheus 或 Zabbix。

## 应用场景

1. 自动化运维巡检流水线
   运维团队可将 Terminus Navigator 部署为内部 Jenkins 流水线的一个阶段，每日定时拉取资源列表并执行 HTTP HEAD 请求，将超时或证书异常的结果推送至企业微信或 Slack 告警通道。该场景下，项目充当外部依赖的“健康探针”。

2. 学术爬虫策略初始化
   研究机构在开展区域性网络内容分析前，可使用本项目的分类索引快速构建种子 URL 集合。通过标签筛选特定语种与区域，减少人工收集时间，同时利用内置的校验功能过滤无效或重定向异常的目标。

3. 网络可观测性实验床
   网络工程团队搭建模拟用户访问环境时，需要稳定的外部测试目标集。Terminus Navigator 提供的资源列表涵盖多种协议（HTTP/HTTPS）与不同响应特征，可用于验证负载均衡策略、DNS 解析逻辑及边缘缓存命中率。

4. 合规性审计辅助
   对于需要定期审查外联策略的企业，项目每日生成的快照报告可作为访问记录的时间戳凭证，辅助审计员核对防火墙策略与实际外联行为的一致性，降低漏审风险。

## 快速开始

以下步骤适用于 Linux/macOS 环境，要求已安装 Git 与 Python 3.9+。

```bash
# 1. 克隆代码仓库
git clone https://github.com/terminus-navigator/tn-core.git
cd tn-core

# 2. 安装依赖项（使用虚拟环境）
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 3. 构建索引并执行本地预览（默认监听 8080 端口）
python build.py --source ./data/sources.yaml --output ./dist
cd dist && python -m http.server 8080
```

访问 `http://127.0.0.1:8080` 即可查看生成的静态导航页面。如需自定义资源列表，请编辑 `data/sources.yaml` 文件，格式参考文件内注释。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Python | 3.9 - 3.11 | 核心构建脚本与校验逻辑运行环境 |
| pip | 22.0+ | 管理 Python 包依赖 |
| Git | 2.25+ | 用于克隆仓库及版本管理 |
| curl | 7.68+ | 部分健康检查脚本依赖外部 curl 调用 |
| make | 3.81+ | 可选，用于自动化任务编排 |
| 网络连接 | 稳定外网 | 用于执行资源可访问性探测（若需） |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/user-guide/` | 如何添加自定义资源、修改分类标签、调整探测超时参数 |
| 运维手册 | `/docs/ops/` | 如何配置 systemd 定时任务、迁移静态文件至 Nginx、设置日志轮转 |
| 开发指南 | `/docs/development/` | 构建流程详解、单元测试编写规范、PR 提交流程 |
| API 参考 | `/docs/api/` | 导出 JSON 数据结构的字段定义、用于二次开发的公共函数签名 |
| 设计决策 | `/docs/design/` | 为什么选择纯静态方案、缓存策略取舍、安全边界考虑 |

## 资源列表

本部分收录了当前版本所引用的全部原始外部定位符，按用途与内容特征分组。每个条目均按用户提供的原始形式原样输出，未做任何协议补全或格式修正。

技术验证类

<code>ribennvyoutuijian.com.cn</code>

<code>guochanzhenshizipai.com.cn</code>

媒体内容索引类

<code>wuyezaixianjuchang.com.cn</code>

<code>rihanzaixianw.org.cn</code>

<code>rihanzaixianshipinguankan.org.cn</code>

字幕与语言资源类

<code>zuixinzhongwenzimuzaixian.org.cn</code>

<code>zhongwenzaixianguankanshipin.org.cn</code>

## 项目结构

```text
tn-core/
├── build.py                # 主构建入口，解析 YAML 并生成 HTML/JSON 输出
├── requirements.txt        # Python 依赖列表 (PyYAML, jinja2, click, requests)
├── Makefile                # 提供 clean, test, deploy 等快捷指令
├── data/
│   ├── sources.yaml        # 核心资源索引表（含标签、校验规则、备注）
│   ├── tags.yaml           # 标签体系定义与层级关系
│   └── whitelist.yaml      # 域名白名单，用于跳过某些自动检测规则
├── scripts/
│   ├── probe.py            # 多线程探测脚本，输出 JSON 格式状态报告
│   ├── diff.py             # 对比两个快照文件，生成变更摘要
│   └── cron_wrapper.sh     # 供 crontab 调用的包装脚本，带日志记录
├── templates/
│   ├── index.j2            # Jinja2 主页面模板
│   ├── detail.j2           # 单条资源详情页模板
│   └── report.j2           # 每日检测报告邮件模板
├── static/
│   ├── css/                # 极简风格样式表（响应式，无外部 CDN）
│   ├── js/                 # 纯前端交互（表格排序、标签筛选）
│   └── fonts/              # 开源字体文件（仅备选）
├── tests/
│   ├── unit/               # 单元测试，覆盖解析器与校验函数
│   └── integration/        # 集成测试，模拟构建全流程
├── dist/                   # 默认构建输出目录（不纳入版本控制）
├── logs/                   # 运行时日志存放位置（不纳入版本控制）
└── README.md               # 当前文件
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增资源分类、优化探测逻辑、改进文档或报告缺陷。请遵循以下流程：

1. 查阅 Issue 列表，确认当前是否已有类似提议或正在处理的工作。若无，请新建一个 Issue 描述您的改动意图，并附上简要的背景说明与预期收益。

2. Fork 本仓库至您的个人空间，在本地新建一个功能分支，分支命名建议使用 `feature/简述` 或 `fix/简述` 格式。

3. 在 `data/sources.yaml` 中按照既定格式添加或修改资源条目。若涉及新增标签，需同步更新 `data/tags.yaml` 文件。所有变更须通过单元测试（执行 `make test`）。

4. 提交 Pull Request 至主仓库的 `main` 分支，PR 描述中请关联对应的 Issue 编号，并附上本地构建成功的截图或日志摘要。维护者会在 3 个工作日内进行审查与反馈。

5. 若提交涉及外部资源可访问性探测逻辑的修改，请额外提供至少 3 个不同网络环境下的测试结果，以确保修改不具有地域偏差。

## 常见问题

**问：项目是否存储或代理任何外部资源的内容？**

答：不存储。项目仅维护资源定位符的文本列表与元数据（如标签、上次验证时间）。所有对外部资源的访问动作均由使用者主动触发，项目本身不提供缓存、代理或内容转发功能。使用者应自行遵守目标服务器的 robots 协议及当地法律法规。

**问：探测脚本会导致目标服务器负载过高吗？**

答：默认配置下，探测使用单线程顺序执行，且每次请求间隔不低于 2 秒。超时时间设置为 5 秒，仅发送 HTTP HEAD 请求，不会下载响应体。对于生产环境的大规模扫描，建议使用者根据自身网络条件调整并发数，并遵守目标域名的服务条款。

**问：如何更新已收录的资源列表？**

答：您可以直接编辑 `data/sources.yaml` 文件，添加、删除或修改条目。修改后重新运行 `build.py` 即可生成新的静态输出。若希望定期同步上游官方列表，可编写自定义脚本替换该 YAML 文件，并通过 cron 触发自动构建。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:12
