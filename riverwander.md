# Navigator Hub

Navigator Hub 是一个面向技术团队与内容运营者的轻量级导航资源聚合平台，专为解决“信息源分散、书签管理混乱、团队共享困难”而设计。项目定位为技术资源与外链汇总站，通过结构化分类与无冗余的呈现方式，帮助用户将零散的 URL、文档、视频链接转化为可维护、可检索、可协作的知识导航体系。

目标用户包括开源社区维护者、技术内容运营人员、研发团队内部知识库管理员，以及任何希望对自己收藏的线上资源进行系统化梳理的个人或组织。Navigator Hub 不提供内容存储，仅聚焦于链接的元数据管理、分类展示与状态监控，确保用户始终能够快速定位到有效的线上资源。

## 功能概览

- **多级分类目录系统**：支持无限层级的资源分类，便于按业务线、技术栈或使用频率组织链接，每个分类可单独设置图标与描述。

- **链接健康状态检测**：每日自动检测所有外链的可访问性，对失效链接进行标记并生成报告，避免用户点击无效地址。

- **全文元数据检索**：基于标题、标签、分类路径、描述说明进行快速检索，支持模糊匹配与精确筛选，提升资源查找效率。

- **资源访问统计面板**：记录每个链接的点击次数、最近访问时间与访问来源，帮助团队识别高频使用的核心资源。

- **批量导入与导出**：支持从 CSV、JSON、HTML 书签文件批量导入链接数据，同时支持将当前导航结构导出为多种格式用于备份或迁移。

- **团队协作注解**：允许成员在链接下方添加注解或使用说明，支持版本记录与修改人追溯，便于知识传递。

- **响应式前端界面**：采用移动优先的布局策略，在桌面与移动设备上均能获得良好的浏览与操作体验，适配日常办公与远程查阅场景。

- **开放 API 接口**：提供 RESTful 风格的 API 用于链接的增删改查及分类管理，方便与其他内部系统进行集成。

## 应用场景

- **技术团队内部知识库导航**：研发团队可将常用技术文档、API 参考、设计规范、CI/CD 流水线地址统一收录，按项目或领域分类，新成员入职时可快速了解团队依赖的关键资源。

- **开源项目文档聚合页**：开源维护者可将项目相关的代码仓库、问题追踪、邮件列表、会议记录、视频教程等资源聚合为一个导航页面，降低社区贡献者的学习成本。

- **个人学习路线管理**：学习者可按照技术方向（如前端、后端、运维、算法）建立自己的学习资源导航，配合状态标记记录学习进度，定期回顾与更新。

- **内容运营资源调度**：内容团队可将选题库、设计素材平台、社交媒体管理后台、数据报表工具等纳入统一导航，减少在不同标签页之间切换的时间损耗，提升协同效率。

- **活动与会议资料索引**：针对线上峰会、黑客松、培训营等临时性活动，快速搭建包含日程、直播入口、课件下载、互动问卷等链接的专题导航页面，活动结束后可归档保留。

## 快速开始

以下步骤适用于开发者本地部署与运行 Navigator Hub 服务端。

```bash
# 克隆项目仓库
git clone https://github.com/navigator-hub/navigator-hub.git

# 进入项目目录
cd navigator-hub

# 安装依赖（使用 npm）
npm install

# 配置环境变量，复制示例配置文件
cp .env.example .env

# 初始化数据库（SQLite 默认）
npm run db:init

# 运行开发服务器
npm run dev
```

启动成功后，访问 <code>http://localhost:3000</code> 即可进入导航管理界面，默认管理员账号为 <code>admin@navigator.local</code>，初始密码在首次启动时输出于控制台日志中，请及时修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，推荐使用 nvm 管理版本 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖 |
| SQLite | 3.35 及以上 | 默认内置数据库，无需额外安装，生产环境可切换为 PostgreSQL |
| Redis | 7.x（可选） | 用于缓存与会话存储，非必需但建议生产环境启用 |
| Nginx | 1.22 及以上（可选） | 推荐作为反向代理服务器，提供静态资源缓存与负载均衡 |
| Git | 2.30 及以上 | 版本控制工具，用于克隆与更新代码 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user-guide/ | 如何创建分类、添加链接、导入导出数据、使用检索功能 |
| 管理员指南 | /docs/admin-guide/ | 用户权限管理、系统配置项、健康检测参数调优、备份恢复策略 |
| API 参考 | /docs/api-reference/ | 所有开放接口的请求路径、参数说明、响应格式与错误码定义 |
| 部署运维 | /docs/deployment/ | 生产环境部署步骤、容器化方案、监控指标接入与日志配置说明 |
| 开发指南 | /docs/development/ | 项目架构概述、目录结构说明、代码规范、测试流程与提交规则 |
| 变更日志 | /docs/changelog/ | 每个版本发布的新功能、修复问题、破坏性变更及升级注意事项 |

## 资源列表

以下为 Navigator Hub 推荐收录的中文视频与媒体资源导航条目，供用户参考或直接导入使用。所有链接均按原始来源原样列出。

影视与视频资源分类

<code>zhongwenzimuzhuanqu.com.cn</code>

<code>zhongwenzimuzaixianshipinguankan.com.cn</code>

<code>zhongwenzimugaoqingzaixianguankan.com.cn</code>

<code>zhongwenzimuzaixianbofangshipin.com.cn</code>

<code>zaixianzhongwenzimushipin.com.cn</code>

<code>renqizaixianmianfeishipin.com.cn</code>

<code>zhongwenzaixianmianfeishipin.com.cn</code>

## 项目结构

```
navigator-hub/
├── backend/                          # 后端服务代码目录
│   ├── src/
│   │   ├── controllers/              # 控制器层，处理请求与响应
│   │   ├── services/                 # 业务逻辑服务，包含链接检测、统计等核心功能
│   │   ├── models/                   # 数据模型定义（分类、链接、用户、访问日志）
│   │   ├── middlewares/              # 中间件：鉴权、日志、限流、错误处理
│   │   ├── routes/                   # 路由定义，映射 URL 路径到控制器
│   │   ├── utils/                    # 工具函数：URL 解析、日期格式化、校验器
│   │   └── workers/                  # 后台任务：健康检测、统计聚合、邮件通知
│   ├── tests/                        # 单元测试与集成测试用例
│   └── config/                       # 配置文件（数据库连接、Redis 参数、日志级别）
├── frontend/                         # 前端单页应用代码目录
│   ├── src/
│   │   ├── components/               # Vue/React 组件库（导航树、搜索栏、统计面板）
│   │   ├── pages/                    # 页面级组件（首页、分类视图、管理后台）
│   │   ├── stores/                   # 状态管理（用户状态、导航数据、筛选条件）
│   │   ├── api/                      # 与后端通信的接口封装
│   │   └── styles/                   # 全局样式与主题变量
│   └── public/                       # 静态资源（favicon、manifest、robots.txt）
├── docs/                             # 项目文档（用户手册、API 文档、部署指南）
│   ├── user-guide/
│   ├── admin-guide/
│   ├── api-reference/
│   └── deployment/
├── scripts/                          # 运维与开发辅助脚本（数据库迁移、种子数据、备份）
├── docker/                           # Docker 构建文件与 compose 编排
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   └── docker-compose.yml
├── .env.example                      # 环境变量配置模板
├── .gitignore                        # Git 忽略文件列表
├── package.json                      # 项目元信息与依赖声明
├── package-lock.json                 # 依赖版本锁定文件
├── README.md                         # 项目概述文档（当前文件）
└── LICENSE                           # MIT 许可证文本
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，无论是代码、文档、测试还是建议反馈。请遵循以下步骤参与项目开发。

第一步：阅读项目行为准则与贡献规范，确保理解社区沟通方式与代码提交流程。相关文档位于 <code>/docs/development/code-of-conduct.md</code>。

第二步：在 Issue 列表中查找标记为 <code>good-first-issue</code> 或 <code>help-wanted</code> 的条目，或自行提交新 Issue 说明拟解决的问题或新增功能，等待维护者确认。

第三步：Fork 本仓库至个人账户，在本地基于 <code>main</code> 分支创建新的功能分支，分支命名建议采用 <code>feature/描述</code> 或 <code>fix/描述</code> 格式。

第四步：完成代码修改后，确保通过全部单元测试与 ESLint 校验，并补充或更新对应的测试用例与文档说明。提交信息应遵循约定式提交规范（Conventional Commits）。

第五步：向主仓库的 <code>main</code> 分支发起 Pull Request，填写 PR 模板中的检查清单，描述改动内容、影响范围及测试情况。PR 需要至少一位维护者审核通过后方可合并。

## 常见问题

**问：Navigator Hub 是否支持私有链接或内网地址的存储？**

答：支持。系统不对链接域名做任何限制，内网 IP、localhost 或非公网域名均可正常保存与分类。但需注意健康检测功能将无法检测内网地址的可访问性，检测结果会标注为“不可检测”状态。建议将私有链接单独放入不启用健康检测的分类中。

**问：如何将现有的浏览器书签批量迁移到 Navigator Hub？**

答：您可以从浏览器导出书签为 HTML 文件，然后在 Navigator Hub 管理后台的“导入”功能中选择 HTML 格式上传。系统会自动解析书签文件夹结构并转换为分类与链接条目。同时支持 CSV 与 JSON 格式导入，便于从其他导航工具迁移数据。导入后请检查分类层级与链接标题是否准确，必要时可手动调整。

**问：生产环境下如何确保链接健康检测不会对目标站点造成压力？**

答：系统默认采用分布式检测策略，检测超时设为 5 秒，并发请求数限制为 10 个，且每天仅检测一次。检测间隔在配置文件中可调，建议对内部或关键外部链接设置更长的检测周期。此外，检测请求会携带特定的 User-Agent 标识，便于目标站点识别为自动化检测流量，您也可以在配置中关闭检测功能或仅对特定分类启用。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:56
