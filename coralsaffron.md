# LinkHarbor

LinkHarbor 是一个面向技术社区与独立开发者的高质量外链聚合与导航系统。项目定位于解决信息碎片化时代优质技术资源难以发现、难以归类、难以持久访问的核心痛点。LinkHarbor 并非简单的书签管理器，而是一个具备健康检查、访问统计、分类标签系统与社区共建能力的轻量级外链枢纽。目标用户包括个人开发者、技术博主、开源项目维护者以及中小型技术团队，帮助其在庞杂的网络环境中快速定位并复用经过验证的在线资源。

LinkHarbor 以静态站点形态交付，兼容 GitHub Pages、Vercel、Netlify 等主流托管平台，同时提供 Docker 自托管方案。系统核心采用 Go 语言编写后端索引生成器，前端使用原生 JavaScript 与 CSS，确保在不依赖重型框架的前提下实现秒级页面加载与流畅的筛选交互体验。

## 功能概览

- **智能分类与标签系统**：支持为每个外链资源分配多个维度标签（如“影视”、“社交”、“工具”、“文档”），并支持组合筛选，帮助用户在数百条链接中快速定位目标类别。

- **自动化健康检查**：后端定时任务每小时对收录的全部外链发起 HTTP HEAD 请求，检测可达性、响应时间与 HTTP 状态码，异常链接将自动标记并移入待审核区，确保导航列表始终包含有效资源。

- **访问量统计与热度排序**：基于轻量级点击计数机制，记录每个外链的累计点击次数与近七日点击趋势，支持按热度、新增时间、字母序等多种方式排序，凸显社区活跃资源。

- **用户提交与审核工作流**：开放用户提交新链接入口，提交内容进入后台审核队列。审核通过后自动生成分类建议并合并至主索引，形成社区驱动的资源增长模式。

- **全文检索与模糊匹配**：内置倒排索引检索能力，支持对标题、描述、标签、域名进行关键词搜索，并支持拼写容错与拼音首字母匹配，提升查找效率。

- **响应式布局与暗色主题**：页面布局适配桌面、平板与移动设备，并跟随系统偏好自动切换浅色/深色主题，保证不同使用环境下的阅读舒适度。

- **数据导出与备份接口**：提供 RESTful API 接口允许管理员导出完整链接数据为 JSON 或 CSV 格式，便于迁移、二次分析或与其他系统集成。

## 应用场景

- **技术团队内部知识库导航**：开发团队可将 LinkHarbor 部署为内部研发环境的统一入口，集中收录常用开发文档、API 参考、CI/CD 工具地址、监控面板等，减少成员在多个标签页间切换查找的时间成本。

- **开源项目推荐页替代方案**：开源项目维护者可使用 LinkHarbor 构建项目生态推荐页，集中展示周边工具、插件、示例项目及相关社区讨论区的链接，替代传统的散落在 README 中的无序外链列表。

- **垂直领域资源聚合站**：针对特定技术领域（如云原生、机器学习、前端工程化）建立专属导航实例，通过标签筛选与热度排序帮助从业者跟踪领域内高频使用的新工具与新平台。

- **个人开发者书签公开化**：个人开发者可将自己长期积累的技术书签以 LinkHarbor 实例形式公开分享，既方便多设备同步，也能为其他开发者提供参考价值，形成轻量级个人品牌输出。

- **线下技术沙龙或黑客松资源辅助**：在技术活动期间临时部署 LinkHarbor 作为活动专属资源板，集中放置活动日程、演讲资料链接、协作工具地址、Wi-Fi 登录页等，活动结束后可归档保留。

## 快速开始

以下步骤指导您在本地环境中快速启动 LinkHarbor 开发或预览实例。

```bash
# 1. 克隆代码仓库
git clone https://github.com/linkharbor/linkharbor.git
cd linkharbor

# 2. 安装 Go 依赖与前端构建工具
go mod download
npm install --global esbuild

# 3. 构建前端静态资源与后端二进制文件
make build

# 4. 初始化示例数据（包含预置分类与演示链接）
./bin/linkharbor init --sample-data

# 5. 启动服务，默认监听 8080 端口
./bin/linkharbor serve --port 8080
```

启动后，访问 http://localhost:8080 即可查看本地实例。管理员后台默认路径为 /admin，初始账号密码请在启动日志中查看临时生成凭证。

## 安装要求

部署 LinkHarbor 需要满足以下基础环境依赖，请根据部署方式准备对应组件。

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Go 编译器 | 1.21 或更高 | 用于编译后端索引生成器与服务进程，二进制分发版可免去此依赖 |
| SQLite | 3.35 及以上（嵌入式） | 作为默认元数据存储，无需额外安装，直接嵌入二进制中 |
| Node.js | 18.x 或 20.x LTS | 仅构建前端资源时需要，生产运行无需 Node 环境 |
| esbuild | 0.19.x 或更新 | 用于打包 JavaScript 与 CSS，可通过 npm 全局安装 |
| Docker（可选） | 20.10 及以上 | 若使用容器化部署，需安装 Docker 引擎与 Compose 插件 |
| 操作系统 | Linux / macOS / Windows | 二进制跨平台支持，Linux 推荐内核 5.0+，macOS 11+，Windows 10 1903+ |

## 文档导航

为帮助不同角色的使用者快速获取所需信息，LinkHarbor 文档体系按层面划分如下：

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | /docs/user-guide/ | 如何添加链接、如何筛选搜索、如何提交新资源、如何管理个人收藏等日常操作。 |
| 管理员手册 | /docs/admin-guide/ | 如何审核用户提交、如何配置健康检查间隔、如何调整排序权重、如何执行数据备份与恢复。 |
| 部署参考 | /docs/deployment/ | 如何在 VPS、容器、云函数、静态托管平台等不同环境中部署 LinkHarbor，含环境变量说明。 |
| 贡献者指引 | /docs/contributing/ | 如何报告缺陷、如何提交新功能提案、代码规范、提交信息格式要求以及本地测试流程。 |

## 资源列表

本导航实例收录了多个类别的在线资源，以下按类别分组列出全部已收录链接。所有链接均按照用户原始输入原样呈现。

### 影视与在线播放

- <code>gaoqingpianyuanzaixianbofang.com.cn</code>

### 社交与交友服务

- <code>fujinderenyueai.com.cn</code>
- <code>shenyeshangmen.com.cn</code>
- <code>jiaoyouyiyeqing.com.cn</code>
- <code>moliaoyue.com.cn</code>
- <code>tongchengyue.com.cn</code>
- <code>yueaiwang.com.cn</code>

## 项目结构

LinkHarbor 代码仓库遵循标准 Go 项目布局，同时融合前端资源与配置模板。以下为项目主要目录与文件说明：

```
linkharbor/
├── cmd/                           # 命令行入口
│   ├── serve/                     # 服务启动命令 (serve)
│   │   └── main.go                # 解析端口、配置文件，启动 HTTP 服务
│   ├── init/                      # 初始化命令 (init)
│   │   └── main.go                # 生成配置文件、创建数据库、导入示例数据
│   └── admin/                     # 管理员工具命令 (admin)
│       └── main.go                # 数据迁移、审核、导出等运维操作
├── internal/                      # 内部包，不对外暴露
│   ├── core/                      # 核心业务逻辑
│   │   ├── link.go                # 链接结构体定义、分类与标签处理
│   │   ├── health.go              # 健康检查调度器与状态更新
│   │   └── stats.go               # 点击统计、热度计算与缓存管理
│   ├── storage/                   # 存储层抽象
│   │   ├── sqlite.go              # SQLite 实现，包含建表与 CRUD 操作
│   │   └── interface.go           # 存储接口定义，便于扩展其他数据库
│   ├── web/                       # Web 处理层
│   │   ├── handler.go             # 路由注册与中间件绑定
│   │   ├── api.go                 # RESTful API 端点实现
│   │   └── template.go            # 服务端渲染模板辅助函数
│   └── config/                    # 配置加载与校验
│       └── config.go              # 支持 YAML 文件、环境变量与默认值
├── web/                           # 前端静态资源
│   ├── assets/                    # 图片、图标、字体等原始素材
│   │   └── logo.svg               # 项目 Logo 矢量图
│   ├── css/                       # 样式源码
│   │   ├── main.css               # 全局布局与公共样式
│   │   ├── dark.css               # 暗色主题变量与覆盖
│   │   └── responsive.css         # 响应式断点处理
│   ├── js/                        # JavaScript 模块
│   │   ├── app.js                 # 应用入口，初始化筛选、渲染与事件监听
│   │   ├── filter.js              # 分类与标签组合筛选逻辑
│   │   ├── search.js              # 全文检索与高亮匹配
│   │   └── stats.js               # 点击上报与热度显示更新
│   └── templates/                 # 服务端渲染 HTML 模板
│       ├── index.html             # 主页面模板，含列表与侧栏
│       └── admin.html             # 管理后台页面模板
├── configs/                       # 配置示例与默认策略
│   ├── default.yaml               # 默认配置（端口、检查间隔、分页大小等）
│   └── security.yaml.example      # 安全策略示例（CORS、速率限制等）
├── scripts/                       # 构建与辅助脚本
│   ├── build.sh                   # 全量构建入口，调用前端编译与 Go 构建
│   ├── test.sh                    # 单元测试与集成测试执行脚本
│   └── docker-entrypoint.sh       # Docker 镜像入口脚本
├── testdata/                      # 测试辅助数据
│   ├── sample_links.json          # 示例链接数据（用于初始化与测试）
│   └── mock_responses/            # 健康检查模拟响应数据
├── Dockerfile                     # 多阶段构建 Docker 镜像定义
├── docker-compose.yml             # 本地开发与演示容器编排
├── go.mod                         # Go 模块依赖定义
├── go.sum                         # Go 依赖校验和
├── Makefile                       # 常用任务快捷命令（build/test/clean）
└── README.md                      # 项目入口文档（本文件）
```

## 贡献指南

LinkHarbor 欢迎社区提交各类贡献，包括但不限于代码修复、新功能、文档改进以及新资源链接的推荐。请遵循以下步骤参与贡献：

1. 在 GitHub 上 Fork 本项目至个人账户，并克隆本地开发环境。确保本地 Go 版本与 Node.js 版本满足安装要求，并执行 `make pre-commit` 安装代码格式化钩子。

2. 创建新分支进行开发，分支命名遵循 `feat/描述`、`fix/描述` 或 `docs/描述` 格式。提交信息请使用英文，采用 `<类型>: <简短说明>` 结构，例如 `fix: correct health check timeout for slow endpoints`。

3. 若涉及代码变更，需在 `internal/` 对应包下补充单元测试，确保测试覆盖率达到 80% 以上。执行 `make test` 运行全部测试套件，确保本地所有测试通过。

4. 完成开发后，提交 Pull Request 至主仓库的 `main` 分支。PR 描述中需清晰说明变更目的、实现方式以及影响范围，并关联相关 Issue（如有）。

5. 新增外链资源的推荐请通过管理员后台提交表单或使用 `./bin/linkharbor admin submit --url <链接>` 命令行方式提交，提交时请附带资源描述、推荐分类与标签，便于审核人员评估。

## 常见问题

**问：健康检查误报链接不可达，如何处理？**

答：部分站点可能启用防爬策略或对 HEAD 请求返回非标准状态码。您可在配置文件 `configs/default.yaml` 中调整 `health.check_method` 为 `GET`，或延长 `health.timeout_seconds` 至 10 秒。同时支持配置 `health.ignore_status_codes` 列表，将特定状态码（如 403、429）视为可达。若误报持续，可在管理后台手动将该链接标记为“已验证”并暂停自动检查 24 小时。

**问：如何迁移 LinkHarbor 实例到另一台服务器？**

答：迁移时需复制两个关键内容：SQLite 数据库文件（默认位于 `data/linkharbor.db`）与配置文件 `configs/default.yaml`。若使用自定义存储路径，请同时迁移该目录。在新服务器上部署相同版本的二进制文件后，将数据库文件与配置文件放置于对应位置，启动服务即可。如需跨架构迁移（如 x86 转 ARM），建议使用 Docker 镜像方式运行，直接挂载数据卷即可实现无缝迁移。

**问：用户提交的链接包含恶意内容或违反使用政策，如何防范？**

答：系统默认开启链接提交审核工作流，所有新提交链接初始状态为“待审核”，不会对外展示。管理员审核时可通过内置的 `./bin/linkharbor admin check --url` 命令调用第三方安全检测 API（需配置 `security.vendor_api_key`）。同时支持配置关键词黑名单，在提交阶段对标题、描述、域名进行过滤拦截。建议生产环境开启管理员双人审核模式，由两名管理员独立确认后方可通过。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-21 22:29:55
